# Database settings
DB_NAME=posts_db
DB_USER=app_user
DB_PASS=your_password

# For local development (set DB_HOST to localhost and use port forwarding or direct connection)
DB_HOST=localhost

# For Cloud Run deployment (uncomment and set your connection name)
# INSTANCE_CONNECTION_NAME=your-project-id:us-central1:crud-db
